# Mini-Nginx на asyncio и потоках

reverse proxy для HTTP/1.1 с двумя реализациями сетевого слоя:
на `asyncio` и на блокирующих сокетах с пулом потоков.

## Текущее состояние

Уже реализовано:

- приём TCP-соединений;
- минимальный парсинг стартовой строки и заголовков HTTP-запроса;
- потоковая передача тела запроса без полной загрузки в память;
- потоковая передача ответа upstream клиенту;
- backpressure через `await writer.drain()` в asyncio-реализации и блокирующий
  `socket.sendall()` в реализации на потоках;
- round-robin между несколькими upstream;
- лимит клиентских соединений и соединений к каждому upstream;
- connect/read/write таймауты и общий таймаут запроса в asyncio-реализации;
- YAML-конфигурация;
- клиентский HTTP keep-alive;
- переиспользование TCP-соединений к upstream;
- парсинг заголовков ответа и логирование HTTP-статуса;
- структурированные логи через `structlog`;
- минимальные счётчики и таймеры;
- нагрузочное тестирование через k6.

Asyncio-реализация обслуживает соединения корутинами в одном event loop.
Реализация на потоках назначает клиентское соединение потоку из
`ThreadPoolExecutor` и дополнительно умеет запускать несколько процессов.

## Структура проекта

```text
proxy/
  ...                 реализация на asyncio
  main.py             точка запуска приложения
  config.py           модели конфигурации и загрузка YAML
  config.yml          адрес proxy, upstream, таймауты и лимиты
  proxy_server.py     TCP-сервер и лимит клиентских соединений
  client_handler.py   обработка и проксирование HTTP-запроса
  upstream_pool.py    round-robin и лимит запросов к upstream
  timeouts.py         перевод таймаутов из миллисекунд в секунды
  metrics.py          состояние минимальных метрик
  utils/
    http.py           минимальные HTTP request/response head
    logs.py           настройка логирования
proxy_threads/
  main.py             запуск одного или нескольких worker-процессов
  config.py           модели конфигурации, включая число worker-процессов
  config.yml          отдельная конфигурация реализации на потоках
  proxy_server.py     TCP-сервер и ThreadPoolExecutor
  client_handler.py   синхронная обработка запросов через socket
  upstream_pool.py    потокобезопасные round-robin и пул соединений
  timeouts.py         таймауты блокирующих сокетов
  metrics.py          состояние минимальных метрик
  utils/
    http.py           синхронный HTTP request/response head
    logs.py           настройка логирования
tests/
  echo_app.py         тестовый upstream на FastAPI
  load_test.js        сценарии k6
  load_scenarios.md   запуск и разбор нагрузочных тестов
```

В обеих реализациях `UpstreamPool` выбирает upstream по round-robin, ограничивает
число занятых соединений семафором и возвращает исправное keep-alive соединение
в пул после обработки запроса.

## Как проходит запрос

1. `ProxyServer` принимает TCP-соединение клиента.
2. Клиент занимает место в `max_client_conns`.
3. `HttpRequestHead` читает стартовую строку и заголовки до пустой строки.
4. `UpstreamPool` выбирает upstream по round-robin и захватывает его семафор.
5. Proxy открывает TCP-соединение к выбранному upstream.
6. Заголовки и тело запроса отправляются upstream.
7. Ответ сразу передаётся клиенту кусками по мере получения.
8. Если upstream разрешил keep-alive, соединение возвращается в пул для
   следующего запроса.
9. Клиентское соединение остаётся открытым для следующего запроса либо
   закрывается.

В asyncio-реализации, пока корутина ожидает данные сокета, event loop может
обслуживать другие соединения в том же потоке. `writer.drain()` ограничивает
рост буфера, если получатель читает медленнее отправителя.

В реализации на потоках `accept()` передаёт каждое клиентское соединение в
`ThreadPoolExecutor`. Блокирующие `recv`/`read` и `sendall` освобождают GIL на
время сетевого I/O, поэтому другие потоки продолжают обслуживать свои
соединения. `BoundedSemaphore` ограничивает число клиентов и соединений к
upstream, а `Lock` защищает round-robin и списки свободных соединений.

## Конфигурация

Пример [proxy/config.yml](./proxy/config.yml):

```yaml
listen: "127.0.0.1:8090"
upstreams:
  - host: "127.0.0.1"
    port: 9002
  - host: "127.0.0.1"
    port: 9003
timeouts:
  connect_ms: 1000
  read_ms: 15000
  write_ms: 15000
  total_ms: 30000
limits:
  max_client_conns: 1000
  max_conns_per_upstream: 100
```

Таймауты задаются в миллисекундах, а `TimeoutPolicy` переводит их в секунды.
Asyncio-реализация передаёт их в `asyncio.wait_for`, реализация на потоках —
в методы блокирующих сокетов.

Реализация на потоках читает
[proxy_threads/config.yml](./proxy_threads/config.yml). Формат совпадает, но
дополнительно можно задать число worker-процессов:

```yaml
workers: 1
```

Каждый worker создаёт собственный TCP-сервер с `SO_REUSEPORT`, пул потоков и
пул соединений к upstream.

## Локальный запуск

Установить зависимости:

```bash
uv sync
```

Запустить первый upstream:

```bash
INSTANCE_ID=upstream-1 uv run uvicorn tests.echo_app:app \
  --host 127.0.0.1 --port 9002
```

Во втором терминале запустить второй upstream:

```bash
INSTANCE_ID=upstream-2 uv run uvicorn tests.echo_app:app \
  --host 127.0.0.1 --port 9003
```

В третьем терминале запустить asyncio-реализацию:

```bash
uv run -m proxy.main
```

Или запустить реализацию на потоках:

```bash
uv run -m proxy_threads.main
```

Обе реализации слушают один адрес, поэтому одновременно их запускать нельзя.

Проверить GET и POST:

```bash
curl -v http://127.0.0.1:8090/
curl -v -X POST http://127.0.0.1:8090/echo -d 'hello world'
```

При последовательных запросах ответы должны по очереди содержать
`upstream-1` и `upstream-2`.

## Нагрузочное тестирование

Тесты запускаются в Docker-образе k6:

```bash
TEST_TYPE=smoke ./run_k6.sh
TEST_TYPE=load ./run_k6.sh
TEST_TYPE=stress ./run_k6.sh
TEST_TYPE=keepalive ./run_k6.sh
TEST_TYPE=timeout ./run_k6.sh
```

Подробное назначение сценариев, ожидаемые результаты и порядок проведения
тестов описаны в [tests/load_scenarios.md](tests/load_scenarios.md).

### Результаты stress-теста на Ubuntu 24.04

Команда запуска: `TEST_TYPE=stress ./run_k6.sh`.

Сравнение результатов до и после добавления переиспользуемого пула
TCP-соединений к upstream:

| Метрика                        |                   До пула |                После пула |
| ------------------------------ | ------------------------: | ------------------------: |
| Длительность прогона           |              1 мин 10,2 с |                     1 мин |
| Максимальное число VU          |                       200 |                       200 |
| HTTP-запросы                   |                   124 885 |                   179 896 |
| Средняя пропускная способность |        1 778,58 запроса/с |        2 998,21 запроса/с |
| Ошибки HTTP-запросов           |      0,00% (0 из 124 885) |      0,00% (0 из 179 896) |
| Успешные проверки              | 100% (249 770 из 249 770) | 100% (359 792 из 359 792) |
| Среднее время ответа           |                  57,39 мс |                  39,47 мс |
| Медианное время ответа         |                  55,93 мс |                  36,02 мс |
| `p(90)` времени ответа         |                  97,59 мс |                  72,41 мс |
| `p(95)` времени ответа         |                 107,26 мс |                  84,18 мс |
| Максимальное время ответа      |                   10,35 с |                 257,22 мс |
| Запросы к upstream 1           |                    62 445 |                    89 948 |
| Запросы к upstream 2           |                    62 440 |                    89 948 |
| Получено данных                |                     39 МБ |                     52 МБ |
| Отправлено данных              |                     10 МБ |                     15 МБ |

Оба порога stress-профиля пройдены: `p(95) < 1500 мс` и доля ошибок
меньше `5%`. После добавления пула пропускная способность выросла с
1 778,58 до 2 998,21 запроса/с, а `p(95)` уменьшился со 107,26 до 84,18 мс.
Все 179 896 итераций завершились без прерываний, а запросы распределились
между двумя upstream поровну.

### Сравнение asyncio и реализации на потоках при 1000 VU

Команда запуска: `TEST_TYPE=stress ./run_k6.sh`.
CPU и память — приблизительные значения из `htop` во время теста.

| Метрика                        |                   Asyncio |                  1 worker |                       4 workers |
| ------------------------------ | ------------------------: | ------------------------: | ------------------------------: |
| Длительность прогона           |                1 мин 55 с |                1 мин 55 с |                      1 мин 55 с |
| Максимальное число VU          |                     1 000 |                     1 000 |                           1 000 |
| HTTP-запросы                   |                   329 720 |                   229 597 |                         284 872 |
| Средняя пропускная способность |        2 867,09 запроса/с |        1 995,97 запроса/с |              2 476,67 запроса/с |
| Ошибки HTTP-запросов           |      0,00% (0 из 329 720) |      0,00% (0 из 229 597) |            0,00% (6 из 284 872) |
| Успешные проверки              | 100% (659 440 из 659 440) | 100% (459 194 из 459 194) |     99,99% (569 732 из 569 744) |
| Среднее время ответа           |                 265,95 мс |                 382,02 мс |                       307,43 мс |
| Медианное время ответа         |                 265,61 мс |                 417,30 мс |                       276,28 мс |
| `p(90)` времени ответа         |                 455,11 мс |                 536,74 мс |                       582,72 мс |
| `p(95)` времени ответа         |                 492,80 мс |                 575,10 мс |                       645,08 мс |
| Максимальное время ответа      |                 698,90 мс |                    1,04 с |                          1,32 с |
| Запросы к upstream 1           |                   164 860 |                   114 799 |                         142 445 |
| Запросы к upstream 2           |                   164 860 |                   114 798 |                         142 421 |
| Получено данных                |                     96 МБ |                     67 МБ |                           83 МБ |
| Отправлено данных              |                     27 МБ |                     19 МБ |                           24 МБ |
| Использование CPU proxy        |                      ≈80% |                      ≈80% |      ≈20% на worker, ≈80% всего |
| Резидентная память proxy (RSS) |                    ≈39 МБ |                    ≈64 МБ | ≈36 МБ на worker, ≈144 МБ всего |
| Виртуальная память proxy       |                    ≈62 МБ |                     ≈9 ГБ |   ≈4 ГБ на worker, ≈12 ГБ всего |

Все три варианта прошли пороги stress-профиля: `p(95) < 1500 мс` и доля
ошибок меньше `5%`. Asyncio показал наибольшую пропускную способность и
наименьший `p(95)`: его RPS выше результата одного worker на `43,64%`, а
четырёх worker — на `15,76%`.

Четыре worker увеличили пропускную способность реализации на потоках на
`24,08%` относительно одного worker и уменьшили среднее время ответа с
382,02 до 307,43 мс. При этом медленный хвост стал хуже: `p(95)` вырос с
575,10 до 645,08 мс, а 6 из 284 872 запросов завершились ошибкой. Для каждого
такого запроса не прошли обе проверки k6, поэтому в отчёте указано 12
неуспешных проверок.

Четыре worker не увеличили суммарную загрузку CPU: прежние `≈80%`
распределились примерно по `20%` между процессами. При этом их суммарный RSS
составил около 144 МБ против 64 МБ у одного worker и 39 МБ у asyncio.
Виртуальные 9 ГБ одного worker не являются фактическим потреблением RAM:
основную часть этого значения составляет зарезервированное адресное
пространство под стеки потоков.

## Границы минимальной HTTP-поддержки

Проект сознательно не реализует всю спецификацию HTTP/1.1. На этапе MVP:

- тело запроса определяется по `Content-Length`;
- chunked request body не поддерживается;
- HTTPS/TLS не поддерживается;
- upgrade до WebSocket не поддерживается;
- health-check, retry и circuit breaker оставлены на следующие этапы;
- полноценная очистка всех hop-by-hop заголовков пока не реализована.

Эти ограничения следует учитывать при интерпретации нагрузочных тестов.
